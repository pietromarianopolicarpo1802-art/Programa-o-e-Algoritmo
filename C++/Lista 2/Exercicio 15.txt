#include <iostream>
using namespace std;

int main() {
    int v[20], pos=-1;

    for(int i=0;i<20;i++) {
        cin >> v[i];
        if(v[i]<0 && pos==-1) pos=i;
    }

    cout << pos;
}