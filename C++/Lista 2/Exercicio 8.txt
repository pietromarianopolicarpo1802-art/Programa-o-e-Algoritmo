#include <iostream>
using namespace std;

float area(float r) {
    return 3.14 * r * r;
}

int main() {
    float r, soma = 0;

    for(int i = 0; i < 5; i++) {
        cin >> r;
        soma += area(r);
    }

    cout << soma;
}