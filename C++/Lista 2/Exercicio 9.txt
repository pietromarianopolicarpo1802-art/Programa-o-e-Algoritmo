#include <iostream>
using namespace std;

bool vogal(char c) {
    return c=='a'||c=='e'||c=='i'||c=='o'||c=='u';
}

string troca(string s) {
    for(char &c : s)
        if(vogal(c)) c = '*';
    return s;
}

int main() {
    string s;
    cin >> s;

    cout << troca(s);
}