#include <iostream>
using namespace std;

int main() {
    int limite, multiplo;

    cin >> limite;
    cin >> multiplo;

    for (int i = 1; i <= limite; i++) {
        if (i % multiplo == 0)
            cout << i << " ";
    }

    return 0;
}