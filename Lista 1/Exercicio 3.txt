#include <iostream>
using namespace std;

int main() {
    int numero;
    int soma = 0;

    do {
        cin >> numero;

        if (numero >= 0)
            soma += numero;

    } while (numero >= 0);

    cout << "Soma = " << soma;

    return 0;
}