#include <iostream>
using namespace std;

int main() {
    char v[20], x;
    int c=0;

    for(int i=0;i<20;i++) cin >> v[i];
    cin >> x;

    for(int i=0;i<20;i++)
        if(v[i]==x) c++;

    cout << c;
}